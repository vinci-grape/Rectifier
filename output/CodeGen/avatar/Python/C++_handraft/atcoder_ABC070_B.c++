#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <cmath>
#include <algorithm>
#include <list>
using namespace std;

// A structure to represent a point.
struct Point
{
    double x, y;
};

// A structure to represent a line.
struct Line
{
    Point p1, p2;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{
    Point p1, p2;
    Line line;
};

// A structure to represent a point and a line.
struct LineSegment
{