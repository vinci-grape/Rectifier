#include <iostream>
#include <cstdlib>
using namespace std;

int main ( ) {
    int n, a, i, j, k, sum ;
    cin >> n >> a ;
    if ( sum = ( n * ( n + 1 ) / 2 ) ) {
        k = sum / ( n * ( n + 1 ) / 2 ) ;
        a += [ a [ 0 ] ] ;
        if ( sum = ( k - i + j ) % n || sum = ( k - i + j ) / n >= 0 ) {
            cout << "NO" ;
        }
        else {
            cout << "YES" ;
        }
    }
    else {
        cout << "NO" ;
    }
    return 0 ;
}